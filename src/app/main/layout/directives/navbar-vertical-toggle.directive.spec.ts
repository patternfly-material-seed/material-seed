import { NavbarVerticalToggleDirective } from './navbar-vertical-toggle.directive';

describe('NavbarVerticalToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarVerticalToggleDirective();
    expect(directive).toBeTruthy();
  });
});
